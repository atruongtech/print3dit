import { MinIntValidatorDirective } from './min-int-validator.directive';

describe('MinIntValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinIntValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
